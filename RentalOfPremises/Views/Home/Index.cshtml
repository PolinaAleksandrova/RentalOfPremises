@model HomeViewModel

<h1>Best offers</h1>
<div class=" row mt-5 mb-2">
    @{
        foreach (Premises premises in Model.BestOffer)
        {
            @Html.Partial("AllPremises", premises)
        }
    }
</div>
